import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    globalMargin: {
        marginHorizontal: 20
    },
    title: {
        fontSize: 35,
        fontWeight: 'bold',
        color: 'black'
    }
})